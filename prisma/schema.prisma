// prisma/schema.prisma

generator client {
  provider = "prisma-client-js"
  output   = "../generated/prisma"
}

datasource db {
  provider = "postgresql"
  // url はここには書かず、.env または環境変数で渡すのじゃ
}

model User {
  id   Int    @id @default(autoincrement())
  name String
}
